apiVersion: apps/v1
kind: Deployment
metadata:
  name: blog-butterfly-admin-vue
  namespace: blog-butterfly-go
spec:
  replicas: 1
  selector:
    matchLabels:
      app: blog-butterfly-admin-vue
  template:
    metadata:
      labels:
        app: blog-butterfly-admin-vue
    spec:
      containers:
        - name: admin-vue
          image: blog-butterfly-admin-vue:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: blog-butterfly-admin-vue
  namespace: blog-butterfly-go
spec:
  type: NodePort
  selector:
    app: blog-butterfly-admin-vue
  ports:
    - port: 80
      targetPort: 80
      nodePort: 31085
